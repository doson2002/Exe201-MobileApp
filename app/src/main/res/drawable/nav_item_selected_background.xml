<!-- res/drawable/nav_item_selected_background.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="oval">
            <solid android:color="@android:color/transparent" /> <!-- Nền trong suốt để không che biểu tượng -->
            <stroke android:width="3dp" android:color="@color/white"/> <!-- Viền cho hình tròn -->
            <size android:width="60dp" android:height="60dp"/> <!-- Kích thước hình tròn -->
        </shape>
    </item>
</layer-list>